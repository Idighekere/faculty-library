

export enum Role {
    Admin = 'admin',
    Uploader = 'uploader'
}

export enum Semester {
    First = '1st',
    Second = '2nd'
}

export enum BookCategory {
    LectureNote="lectureNote",
    PastQuestion="pastQuestion",
    TextBook="textBook"
}
